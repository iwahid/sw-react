/* import {FilmFormProperties} from "../../components/FilmForm/FilmForm";
import {ExtendedFilmModel} from "../../models/extendedFilmModel";

export const mapFormDataToExtendedFilm = (formFilm: FilmFormProperties, film?: ExtendedFilmModel): ExtendedFilmModel => {
  return {
    created: film ? film.created : new Date().toISOString(),
    edited: new Date().toISOString(),
    title: formFilm.title,
    episodeId: formFilm.episode_id,
    producer: formFilm.producer,
    openingCrawl: formFilm.opening_crawl,
    director: formFilm.director,
    releaseDate: formFilm.release_date,
    characters: formFilm.characters,
    planets: formFilm.planets,
    starships: formFilm.starships,
    species: formFilm.species,
    vehicles: formFilm.vehicles,
    docId: film ? film.docId : '',
  }
}
 */

 /* FIXME: Add a mapper when work with a films */
export const temp:[] =[]